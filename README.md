# Broker-Free Marketplace — Starter (Hybrid: Products + Services)

This zip contains:
- backend/ : FastAPI starter implementing wallet + manual payouts (Option D)
- frontend/ : a minimal React + Vite scaffold connected to the backend endpoints

Download and run the backend locally; then connect your React frontend to the API.

If you'd like, I can also generate a full React scaffold in this zip (tell me to include it) — for now I kept the frontend minimal to keep the archive small.
